# Prod flavor — the live cloud Supabase project.
# Fill with the real project values (Supabase → Project Settings → API) before
# building the prod flavor. The publishable key is client-safe.
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-prod-key-here
